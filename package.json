{
  "name": "evidenceos-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "db:up": "docker compose up -d",
    "db:up:optional": "node scripts/db-up-optional.cjs",
    "bootstrap:admin": "npm --prefix server run bootstrap:admin",
    "dev:all": "npm run db:up && npm run dev",
    "dev:full": "npm run db:up:optional && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
